---
import { siteConfig } from '../lib/config';
---

<div class="affiliate-disclosure-banner">
  <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
  <span>As an Amazon Associate, {siteConfig.brand} earns from qualifying purchases. Prices and availability are subject to change. <a href="/about/#affiliate-disclosure">Learn about our affiliate policy</a>.</span>
</div>

<style>
  .affiliate-disclosure-banner {
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    font-size: 0.8125rem;
    line-height: 1.5;
    color: hsl(var(--muted-foreground));
    background: hsl(var(--muted) / 0.3);
    border: 1px solid hsl(var(--border) / 0.5);
    border-radius: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .affiliate-disclosure-banner svg {
    flex-shrink: 0;
    margin-top: 0.125rem;
    color: hsl(var(--primary) / 0.6);
  }

  .affiliate-disclosure-banner a {
    color: hsl(var(--primary));
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .affiliate-disclosure-banner a:hover {
    color: hsl(var(--primary-warm));
  }
</style>
