---
import ProductImage from './ProductImage.astro';
import type { Product } from '../lib/config';
import { getAffiliateUrl } from '../lib/config';

interface Props {
  product: Product;
}

const { product } = Astro.props;
const affiliateUrl = getAffiliateUrl(product.asin);

// Build specs list dynamically from available product data
const specEntries: { label: string; value: string }[] = [];

if (product.specs.filtrationStages)
  specEntries.push({ label: 'Stages', value: `${product.specs.filtrationStages}-Stage` });
if (product.specs.filtrationTechnology)
  specEntries.push({ label: 'Technology', value: product.specs.filtrationTechnology });
if (product.specs.packSize)
  specEntries.push({ label: 'Pack Size', value: product.specs.packSize });
if (product.specs.capacity)
  specEntries.push({ label: 'Capacity', value: product.specs.capacity });
if (product.specs.flowRate)
  specEntries.push({ label: 'Flow Rate', value: product.specs.flowRate });
if (product.specs.micronRating)
  specEntries.push({ label: 'Micron Rating', value: `${product.specs.micronRating}μm` });
if (product.specs.filterLife)
  specEntries.push({ label: 'Filter Life', value: product.specs.filterLife });
if (product.specs.pureToWaste)
  specEntries.push({ label: 'Pure:Drain', value: product.specs.pureToWaste });
if (product.specs.compatibility)
  specEntries.push({ label: 'Fits', value: product.specs.compatibility });
if (product.specs.certifications)
  specEntries.push({ label: 'Certified', value: product.specs.certifications });

// Cap at 6 specs for a clean layout
const specs = specEntries.slice(0, 6);
---

<div class="product-hero">
  <div class="hero-image">
    <ProductImage alt={product.name} aspect="square" slug={product.slug} />
  </div>

  <div class="hero-details">
    <!-- Rating + Price header -->
    <div class="hero-topline">
      <div class="hero-rating">
        <span class="star-icon" aria-hidden="true">★</span>
        <span class="rating-value">{product.rating}</span>
        <span class="rating-max">/5</span>
      </div>
      <div class="hero-price">${product.price}</div>
    </div>

    <!-- Key Specs Grid -->
    <div class="hero-specs">
      {specs.map(s => (
        <div class="hero-spec">
          <span class="hero-spec-label">{s.label}</span>
          <span class="hero-spec-value">{s.value}</span>
        </div>
      ))}
    </div>

    <!-- Verdict -->
    <div class="hero-verdict">
      <div class="verdict-heading">Our Verdict</div>
      <p class="verdict-text">{product.verdict}</p>
      <div class="verdict-bestfor">
        <span class="bestfor-icon" aria-hidden="true">✓</span>
        <span><strong>Best for:</strong> {product.bestFor}</span>
      </div>
    </div>

    <!-- CTA -->
    <a href={affiliateUrl} target="_blank" rel="noopener noreferrer nofollow" class="hero-cta">
      Check Price on Amazon
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17l9.2-9.2M17 17V7H7"/></svg>
    </a>
  </div>
</div>

<style>
  .product-hero {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: start;
    margin: 1.5rem 0 2.5rem;
  }

  /* Remove ProductImage standalone constraints when inside hero */
  .hero-image :global(.product-image-real) {
    max-width: none;
    margin: 0;
  }

  .hero-details {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  /* Rating + Price */
  .hero-topline {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
  }

  .hero-rating {
    display: flex;
    align-items: baseline;
    gap: 0.15rem;
  }

  .star-icon {
    color: hsl(var(--accent));
    font-size: 1.25rem;
    margin-right: 0.25rem;
  }

  .rating-value {
    font-size: 1.5rem;
    font-weight: 800;
    color: hsl(var(--foreground));
  }

  .rating-max {
    font-size: 0.9rem;
    color: hsl(var(--muted-foreground));
    font-weight: 500;
  }

  .hero-price {
    font-size: 1.5rem;
    font-weight: 700;
    color: hsl(var(--foreground));
  }

  /* Key Specs Grid */
  .hero-specs {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.5rem;
    padding: 1rem;
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
    border-radius: 0.75rem;
  }

  .hero-spec {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    padding: 0.4rem 0;
  }

  .hero-spec-label {
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: hsl(var(--muted-foreground));
  }

  .hero-spec-value {
    font-size: 0.85rem;
    font-weight: 600;
    color: hsl(var(--foreground));
    line-height: 1.3;
  }

  /* Verdict */
  .hero-verdict {
    padding: 1rem;
    background: hsl(var(--muted));
    border: 1px solid hsl(var(--border));
    border-radius: 0.75rem;
  }

  .verdict-heading {
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: hsl(var(--primary));
    margin-bottom: 0.5rem;
  }

  .verdict-text {
    font-size: 0.9rem;
    line-height: 1.5;
    color: hsl(var(--foreground));
    margin: 0 0 0.75rem;
  }

  .verdict-bestfor {
    display: flex;
    align-items: flex-start;
    gap: 0.4rem;
    font-size: 0.85rem;
    color: hsl(var(--foreground) / 0.85);
    line-height: 1.4;
  }

  .bestfor-icon {
    color: hsl(142 70% 45%);
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  /* CTA Button */
  .hero-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: linear-gradient(135deg, hsl(var(--accent)), hsl(var(--accent-warm)));
    color: #fff !important;
    font-weight: 700;
    font-size: 0.95rem;
    border-radius: 0.6rem;
    text-decoration: none;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .hero-cta:hover {
    transform: translateY(-1px);
    box-shadow: 0 4px 16px hsl(var(--accent) / 0.3);
  }

  /* Mobile: stack vertically */
  @media (max-width: 768px) {
    .product-hero {
      grid-template-columns: 1fr;
      gap: 1.25rem;
    }

    .hero-image {
      max-width: 320px;
      margin: 0 auto;
    }

    .hero-topline {
      justify-content: flex-start;
      gap: 2rem;
    }
  }
</style>
